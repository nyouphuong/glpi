---
ngày: YYYY-MM-DD
files: duong/dan/file.ext:120-145
commit: <hash HEAD lúc ghi note — lấy bằng `git rev-parse --short HEAD`>
---

# [Kết luận trong MỘT câu, viết như tiêu đề báo]

Ví dụ tiêu đề tốt: "Cột `status` lưu bitmask chứ không phải enum"
Ví dụ tiêu đề tệ: "Tìm hiểu về cột status"

**Phát hiện**
2-4 câu. Nói thẳng cái đang xảy ra. Không kể quá trình mò mẫm.

**Vì sao khó tìm**
Câu quan trọng nhất của note — đây là lý do note này tồn tại.
Nếu không trả lời được câu này thì phát hiện đó hiển nhiên, đừng ghi.

**Neo code**
- `duong/dan/file.ext:130` — chỗ ghi giá trị
- `duong/dan/khac.ext:88` — chỗ đọc ra

**Kiểm chứng lại**
Một lệnh hoặc một thao tác để xác nhận note còn đúng.
